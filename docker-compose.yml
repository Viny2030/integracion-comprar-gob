services:
  app:
    build: .
    container_name: monitor_corrupcion
    volumes:
      - .:/app          # Sincroniza el código y el archivo .docx
      - ./data:/app/data # Carpeta para los reportes Excel
    ports:
      - "8501:8501"     # Puerto de Streamlit
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true